# Required in every environment
JWT_SECRET=change-this-to-a-long-random-string

# Neon Postgres connection string. Leave unset for local dev — the app
# falls back to an embedded local Postgres automatically (see README).
DATABASE_URL=

# Phase 2 (weekly AI statements) — not required to run Phase 1.
ANTHROPIC_API_KEY=
